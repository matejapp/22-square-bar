# Copy to .env. Never commit the real .env.

# Use a relative path so the deployed app inherits the page's protocol
# (avoids mixed-content issues on mobile when the page is auto-upgraded
# from http to https).
VITE_API_URL=/api
